export const getPocket = state => state.editPocketModal.pocket;

export const getWeight = state => state.editPocketModal.weight;

export const hasWeight = state => state.editPocketModal.hasWeight;

export const isOpen = state => state.editPocketModal.isOpen;

export const isLoading = state => state.editPocketModal.isLoading;

export const getSerialNumber = state => state.editPocketModal.serialNumber;
